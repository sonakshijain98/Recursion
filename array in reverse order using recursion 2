#include <iostream>

using namespace std;
void Print(int arr[],int size, int i){
    if(i==size)
     return;
    Print(arr,size,++i);
    cout<<arr[i-1]<<" ";
    
   
}

int main()
{
    int arr[5];
    int size=5;
    int val=5;
    for(int i=0;i<5;i++)
     cin>>arr[i];
    Print(arr,size,0);
    return 0;
}
