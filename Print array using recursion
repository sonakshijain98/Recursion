#include <iostream>
#include <vector>
#include <algorithm>
#include <string>
#include<bits/stdc++.h> 

using namespace std;
void PrintArray(int arr[], int size, int i){
    if(i==size)
      return;
    cout<<arr[i]<<" ";
    PrintArray(arr,size,++i);
}

int main()
{
    int arr[5];
    int size=5;
    for(int i=0;i<5;i++)
     cin>>arr[i];
    PrintArray(arr,size, 0);
    return 0;
}
